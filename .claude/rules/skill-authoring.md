---
description: Enforce use of /skill-creator when generating or updating skills
---

ALWAYS use the `/skill-creator` skill when generating a new skill or updating an existing skill. Never write skill files manually — invoke `/skill-creator` so structure, metadata, and prompt quality are validated consistently.
